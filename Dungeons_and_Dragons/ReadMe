Needs to add to UI.GameBoard a get method, to not create business.Tile arrays on main.
Check all classes, fields, methods, extends and implements were added.
add implementations of methods (some already implemented)
add fighting system
add visitor shtick

Add to CastAbility if in range (like for Boss)
